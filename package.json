{
  "name": "mern-app",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm start",
    "react-dev": "cd client && npm start",
    "test": "cd server && npm test",
    "preinstall": "cd server && npm install",
    "installClient": "cd client && npm install",
    "installServer": "cd server && npm install",
    "install": "npm install concurrently && concurrently \"npm run installClient\" \"npm run installServer\"",
    "build": "cd client && npm run build"
  },
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.2.0"
  }
}
